name: Example Java CI Caller

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  java-ci:
    uses: rg-hermann/workflow-templates/.github/workflows/ci-java.yaml@main
    with:
      java-version: '21'
      build-tool: 'maven'
      enable-coverage: true
      run-codeql: false
      run-dependency-review: true
    secrets: inherit
